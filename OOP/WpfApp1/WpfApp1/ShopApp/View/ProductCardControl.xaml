<UserControl x:Class="WpfApp1.ShopApp.View.ProductCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="180" Height="220" Margin="10">

    <Border CornerRadius="8" Padding="10">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource ControlBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Opacity" Value="1"/>

                <Style.Triggers>
                    <DataTrigger Binding="{Binding StockQuantity}" Value="0">
                        <Setter Property="BorderBrush" Value="Red"/>
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="Opacity" Value="0.6"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6"/>
                        <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,-20" To="0,0,0,0" Duration="0:0:0.6" DecelerationRatio="0.9"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid>
            <StackPanel>
                <Image Source="{Binding MainImagePath}" Height="100" Margin="0,0,0,10" Stretch="Uniform" />
                <TextBlock Text="{Binding Title}" Foreground="{DynamicResource HeaderText}"  TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Category}" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding FinalPrice, StringFormat={}{0:C}}" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource PriceColor}"/>
                <TextBlock Text="{Binding StockQuantity, StringFormat=Остаток: {0} шт.}" Foreground="{Binding StatusColor}" FontSize="12" Margin="0,5,0,0"/>
            </StackPanel>

            <Grid HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-5,-5,0">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasDiscount}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Polygon Points="15,0 30,15 15,30 0,15" Fill="#FF9800" Stroke="DarkOrange" StrokeThickness="1">
                    <Polygon.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.5"/>
                    </Polygon.Effect>
                </Polygon>
                <TextBlock Text="%" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>